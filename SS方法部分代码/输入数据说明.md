# SS 方法实验输入数据说明

## 1. 论文原法输入

- 文件名：`pathmnist_patch.pt`
- 原始路径：`C:\Users\DELL\PycharmProjects\PythonProject25\generated_pts\pathmnist_patch.pt`
- 对应脚本：`SS论文原法代码.py`

必需字段：

- `images`
- `labels`
- `poison_flags`

记录中的预处理方式：

```python
images = obj["images"].float() / 255.0
labels = obj["labels"].numpy().astype(np.int32)
poison_flags = obj["poison_flags"].numpy().astype(np.int32)
```

## 2. 增强版输入

- 文件名：`cifar10_patch.pt`
- 原始路径：`C:\Users\DELL\PycharmProjects\PythonProject25\generated_pts\cifar10_patch.pt`
- 对应脚本：`SS增强版代码.py`

必需字段：

- `images`
- `labels`
- `poison_flags`

记录中的输入规格：

- `images`：`[N, 3, 32, 32]`
- `labels`：`[N]`
- `poison_flags`：`[N]`

记录中的预处理方式：

```python
images = obj["images"].float()
if images.max() > 1:
    images = images / 255.0
labels = obj["labels"].long()
poison_flags = obj["poison_flags"].long()
```

## 3. 统一说明

- 两类脚本都依赖 `.pt` 文件中的 `images`、`labels`、`poison_flags` 三个字段。
- `images` 用于模型输入或特征提取。
- `labels` 用于类别统计、分类学习或按类分析。
- `poison_flags` 用于标识样本是否被投毒，便于评估检测效果。

## 4. GitHub 整理建议

- 如果后续公开仓库，建议将上述绝对路径改写为相对路径。
- 建议在文档中补充示例数据下载方式，避免直接随仓库存放大体积 `.pt` 文件。
- 建议将该文档保留为 Markdown 格式，便于 GitHub 在线阅读。
